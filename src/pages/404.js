import React from 'react'
import Layout from '../component/layout'

const error = () => {
    return (
        <Layout>
            <h1>Error Page</h1>
        </Layout>
    )
}

export default error
